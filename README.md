# Agent (IBKR)
Krótki opis projektu.

## Uruchomienie (Docker)
docker compose up --build

## Uruchomienie (Python)
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app/main.py
